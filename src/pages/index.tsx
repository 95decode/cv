import FollowMouse from "../components/fm";

const HomePage = () => {
  return (
    <div style={{ height: "100vh", position: "relative" }}>
      <FollowMouse />
    </div>
  );
};

export default HomePage;